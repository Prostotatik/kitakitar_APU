rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Временные правила для теста: доступ всем аутентифицированным
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
